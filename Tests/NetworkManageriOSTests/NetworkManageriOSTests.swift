import XCTest
@testable import NetworkManageriOS

final class NetworkManageriOSTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkManageriOS().text, "Hello, World!")
    }
}
